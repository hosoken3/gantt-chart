# プラグイン動作確認手順

## ✅ 現在の状態

以下の修正を完了しました：

1. **フォルダ名を修正**: `ganttchart_sample` → `gantt-chart`
2. **不要なファイルを削除**: 開発用ファイルを削除し、実行に必要な3つのファイルのみ残しました
3. **サンプルノートを作成**: 動作確認用のノートを追加

## 📂 最終的なフォルダ構造

```
sample_gantt/
├── .obsidian/
│   └── plugins/
│       └── gantt-chart/           ← フォルダ名修正済み
│           ├── main.js            ← プラグイン本体
│           ├── manifest.json      ← プラグイン情報
│           └── styles.css         ← デザイン
├── Welcome.md                     ← 説明ファイル
├── ガントチャート サンプル.md      ← サンプル1
└── テスト用.md                     ← サンプル2
```

## 🚀 動作確認手順

### 1. Obsidianでvaultを開く

1. Obsidianを起動
2. 「Open another vault」をクリック
3. `sample_gantt` フォルダを選択

### 2. プラグインを有効化

1. **Settings** (⚙️) を開く
2. 左メニューから **Community plugins** を選択
3. **Restricted mode** を **OFF** にする
4. 下の **Installed plugins** セクションで **Gantt Chart** を見つける
5. トグルスイッチを **ON** にする

### 3. サンプルノートで確認

1. 左サイドバーから「ガントチャート サンプル.md」を開く
2. 右上の **読み取りモード** (📖アイコン) をクリック
3. **ページ全体がガントチャートになっていれば成功！** 🎉

### 4. 確認ポイント

✅ タスク一覧が左側に表示される
✅ カレンダーグリッドが右側に表示される
✅ オレンジ〜赤のグラデーションバーが表示される
✅ 今日の列が青色でハイライトされる
✅ 週末の列が薄い灰色になる

## 🆘 トラブルシューティング

### プラグインが表示されない
- Obsidianを完全に終了して再起動してください
- フォルダ名が `gantt-chart`（ハイフン入り）になっているか確認

### ガントチャートが表示されない
- 読み取りモード（プレビュー）になっているか確認
- Frontmatter が正しく書かれているか確認：
  ```markdown
  ---
  gantt: true
  ---
  ```

### 過去のタスクが見えない
- これは正常な動作です
- このプラグインは今日以降のタスクのみを表示します

## 📝 新しいガントチャートの作り方

1. 新しいノートを作成
2. 最初に以下を記述：
   ```markdown
   ---
   gantt: true
   ---
   ```
3. その下にタスクを記述：
   ```
   タスク名 | 開始日 | 終了日
   ```
4. 読み取りモードで確認

---

**以上で動作確認完了です！** 🎊
